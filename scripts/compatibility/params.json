{
  "seed": "${SEED}",
  "keys": 1000000,
  "tests": 100000,
  "maxProgramSize": 20,
  "crashInterval": 900000,
  "bootstrap": true,
  "rollingUpdate": {
    "n1": "${MODE}",
    "n2": "${MODE}",
    "n3": "${MODE}"
  }
}
