name: Verification Metastore tests

on:
  workflow_dispatch:
  schedule:
    - cron: "12 0 * * *" # 00:12am UTC daily

jobs:
  verification-s3metastore:
    name: Verification Metastore tests
    uses: ./.github/workflows/verification-node.yaml
    secrets: inherit
    with:
      scenario: s3metastore
